<% if notification.item_type == 'User' %>
	<% name = User.find(notification.item_id).name %>
	<%= name %> is now following you<br>
<% elsif notification.item_type == 'Comment' %>
	<% name = Comment.find(notification.item_id).user.name %>
	<%= name %> commented on a post you are following <br>
<% end %>