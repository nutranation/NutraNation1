<% if notification.item_type == 'User' %>
	<% user = User.find(notification.item_id) %>
	<%= link_to user.name, user %> is now following you<br>
<% elsif notification.item_type == 'Comment' %>
	<% comment = Comment.find(notification.item_id) %>
	<% post = %>
	<%= link_to comment.user.name, comment.user %> commented on a <%= link_to 'post', comment.post %> you are following <br>
<% end %>