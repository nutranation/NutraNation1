<%=  @item_type  %>
<%=  @following  %>
<%= form_for(current_user.relationships.where("followed_id = ? AND item_type = ?",  @following.id, @item_type).first,
             :html => { :method => :delete },
             :remote => true) do |f| %>
	<input id="item_type" name="item_type" size="30"  type="hidden" value= <%= @item_type %> />
  <div class="actions"><%= f.submit "Unfollow" %></div>

<% end %>