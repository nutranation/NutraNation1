<li>
  <%= image_tag(user.display_image, :alt => "profile",  :width => "50px", :height => "50px") %>  
  <%= link_to user.name, user %>
  <% if current_user.admin? %>
  |
  <%= link_to "delete", user, :method => :delete, :confirm => "You sure?",
                              :title => "Delete #{user.name}" %>
  <% end %>
</li>