<h1><%=  @tag.name %></h1>
<div id="follow_form">
	<% if current_user.following?(@tag, 'Tag') %>
	  <%= render 'unfollow' %>
	<% else %>
	  <%= render 'follow' %>
	<% end %>
</div>
<%= render 'shared/feed' %>
